---
import { cvData } from '../data/cv';

interface Role {
  position: string;
  startDate: string;
  endDate: string;
  description: string;
  achievements?: string[];
}

interface GroupedExperienceItem {
  company: string;
  location: string;
  roles: Role[];
}

// Agrupar experiencias de WUNDER
const groupedExperience = cvData.experience.reduce<GroupedExperienceItem[]>((acc, exp) => {
  const existing = acc.find((item) => item.company === exp.company && item.company === 'WUNDER');
  if (existing) {
    existing.roles.push({
      position: exp.position,
      startDate: exp.startDate,
      endDate: exp.endDate,
      description: exp.description,
      achievements: exp.achievements
    });
  } else {
    acc.push({
      company: exp.company,
      location: exp.location,
      roles: [{
        position: exp.position,
        startDate: exp.startDate,
        endDate: exp.endDate,
        description: exp.description,
        achievements: exp.achievements
      }]
    });
  }
  return acc;
}, []);
---

<section id="experience" class="experience">
  <div class="container">
    <h2>Experiencia</h2>
    <div class="experience-list">
      {groupedExperience.map((exp) => (
        <div class="experience-item">
          <div class="company-header">
            <h3 class="company">{exp.company}</h3>
            <span class="location">{exp.location}</span>
          </div>
          
          {exp.roles.map((role: Role, index: number) => (
            <div class={`role-container ${index === 0 ? 'first-role' : ''}`}>
              <div class="role-header">
                <h4 class="position">{role.position}</h4>
                <span class="date">{role.startDate} - {role.endDate}</span>
              </div>
              <p class="experience-description">{role.description}</p>
              {role.achievements && role.achievements.length > 0 && (
                <ul class="achievements">
                  {role.achievements.map((achievement: string) => (
                    <li>{achievement}</li>
                  ))}
                </ul>
              )}
            </div>
          ))}
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .experience h2 {
    text-align: center;
    margin-bottom: var(--spacing-md);
  }

  .experience-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
    max-width: 980px;
    margin: 0 auto;
  }

  .experience-item {
    background-color: var(--color-bg-card);
    padding: var(--spacing-md);
    border-radius: var(--border-radius);
    border: 1px solid var(--color-border);
    box-shadow: var(--shadow);
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .experience-item:hover {
    transform: translateY(-2px);
    border-color: var(--color-primary-light);
  }

  .company-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-sm);
    border-bottom: 1px solid var(--color-border);
  }

  .company {
    color: var(--color-primary);
    font-weight: 700;
    font-size: 1.3rem;
    margin: 0;
  }

  .role-container {
    padding-top: var(--spacing-md);
    margin-top: var(--spacing-md);
    border-top: 1px dashed var(--color-border-light);
  }

  .role-container.first-role {
    border-top: none;
    margin-top: 0;
    padding-top: 0;
  }

  .role-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: var(--spacing-sm);
    flex-wrap: wrap;
    gap: var(--spacing-sm);
  }

  .position {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 0;
  }

  .location, .date {
    color: var(--color-text-light);
    font-size: 0.9rem;
  }

  .location {
    font-weight: 500;
  }

  .experience-description {
    color: var(--color-text-light);
    margin-bottom: var(--spacing-sm);
    line-height: 1.7;
  }

  .achievements {
    list-style: none;
    padding-left: var(--spacing-md);
  }

  .achievements li {
    position: relative;
    padding-left: var(--spacing-md);
    margin-bottom: var(--spacing-xs);
    color: var(--color-text-light);
  }

  .achievements li::before {
    content: "▸";
    position: absolute;
    left: 0;
    color: var(--color-primary);
  }

  @media (max-width: 768px) {
    .company-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--spacing-xs);
    }

    .role-header {
      flex-direction: column;
    }
  }
</style>