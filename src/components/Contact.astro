---
import { cvData } from '../data/cv';
---

<section id="contact" class="contact">
  <div class="container">
    <h2>Contacto</h2>
    <div class="contact-content">
      <div class="contact-info">
        <p>¿Interesado en trabajar juntos? ¡No dudes en contactarme!</p>
        <div class="contact-details">
          <div class="contact-item">
            <strong>Email:</strong>
            <a href={`mailto:${cvData.personal.email}`}>{cvData.personal.email}</a>
          </div>
          {cvData.personal.phone && (
            <div class="contact-item">
              <strong>Teléfono:</strong>
              <a href={`tel:${cvData.personal.phone}`}>{cvData.personal.phone}</a>
            </div>
          )}
          <div class="contact-item">
            <strong>LinkedIn:</strong>
            <a href={cvData.personal.linkedin} target="_blank" rel="noopener noreferrer">
              Ver perfil
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    background: transparent;
    position: relative;
  }

  .contact h2 {
    text-align: center;
    margin-bottom: var(--spacing-lg);
  }

  .contact-content {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .contact-info p {
    font-size: 1.1rem;
    color: var(--color-text-light);
    margin-bottom: var(--spacing-lg);
  }

  .contact-details {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .contact-item {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
    padding: var(--spacing-md);
    background-color: var(--color-bg-card);
    border-radius: var(--border-radius);
    border: 1px solid var(--color-border-light);
    transition: all 0.3s ease;
  }

  .contact-item:hover {
    border-color: var(--color-border-green);
    box-shadow: var(--shadow), 0 0 15px rgba(45, 134, 89, 0.2);
    transform: translateY(-3px) scale(1.02);
    background-color: var(--color-bg-card-hover);
  }

  .contact-item strong {
    color: var(--color-primary);
    font-weight: 600;
  }

  .contact-item a {
    color: var(--color-text);
    font-size: 1.1rem;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .contact-item a:hover {
    color: var(--color-primary);
    transform: translateX(5px);
  }
</style>

